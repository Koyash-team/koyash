# Environment — never commit secrets
.env
*.env
.env.*
# ...but DO track the example template (it has no real secrets in it)
!.env.example
!backend/.env.example

# Python
__pycache__/
*.pyc
*.pyo
*.py[cod]
.coverage
htmlcov/

# Virtual environments
.venv/
venv/
backend/.venv/

# Customer-provided dataset — not ours to redistribute, see db/README.md
db/data/Koyash.xlsx

# Customer-authored LLM system prompt — not published in the repo (see
# backend/llm_system_prompt.example.txt). Provide via this file or LLM_SYSTEM_PROMPT env.
backend/llm_system_prompt.txt

# macOS
.DS_Store

# IDE
.idea/
.vscode/
*.iml