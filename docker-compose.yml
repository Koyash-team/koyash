services:
  api:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    environment:
      - MONGODB_URI=${MONGODB_URI}
    volumes:
      - ./backend:/app
    restart: unless-stopped
